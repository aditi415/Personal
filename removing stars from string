class Solution {
public:
    string removeStars(string s) {
        int sz=s.length();
        string ans;
        for(int i=0;i<sz;i++)
        if(s[i]=='*')
          ans.pop_back();
        else
          ans.push_back(s[i]);
       return ans;    
    }
};
